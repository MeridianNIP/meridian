# =====================================================================
# Meridian · Python dependencies · v1.0.0 · 2026-04-18
# =====================================================================
# All versions pinned exactly. Do NOT replace == with >=.
# Meridian team tests every combination before shipping a manifest bump.
# =====================================================================

# --- Web framework ---------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0
python-multipart==0.0.9
jinja2==3.1.3
itsdangerous==2.1.2

# --- Data layer ------------------------------------------------------
sqlalchemy==2.0.36
alembic==1.13.1
psycopg2-binary==2.9.10
redis==5.0.1

# --- Auth / crypto ---------------------------------------------------
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
pyjwt==2.8.0
cryptography==42.0.5
pyotp==2.9.0                # TOTP for MFA
pyhumps==3.8.0

# --- Background / scheduling -----------------------------------------
celery[redis]==5.3.6
celery-redbeat==2.2.0

# --- DNS / networking tools ------------------------------------------
dnspython==2.6.1
requests==2.31.0
httpx==0.27.0
pyshark==0.6                # tcpdump sandbox wrapper
python-whois==0.9.4

# --- Network device automation (SSH + vendor drivers) ---------------
netmiko==4.3.0              # ships paramiko + textfsm + ntc-templates
paramiko==3.4.0             # pinned so transitive upgrades don't surprise us

# --- LDAP / AD -------------------------------------------------------
python-ldap==3.4.4
ldap3==2.9.1
msgraph-sdk==1.1.0          # Entra ID via Microsoft Graph

# --- Infoblox / DDI backends -----------------------------------------
infoblox-client==0.6.0

# --- Mail / SMTP -----------------------------------------------------
aiosmtplib==3.0.1
email-validator==2.1.0.post1

# --- Observability / logging -----------------------------------------
structlog==24.1.0
prometheus-client==0.19.0

# --- Vulnerability scanning / SBOM -----------------------------------
pip-audit==2.7.3
cyclonedx-python-lib==6.4.1
spdx-tools==0.8.2

# --- ACME / certificates ---------------------------------------------
acme==2.9.0                 # same version as system certbot
josepy==1.14.0

# --- Validation / data ------------------------------------------------
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- CLI ---------------------------------------------------------------
click==8.1.7                # meridian-nip CLI; celery pulls this in too, pin explicitly

# --- Testing (dev-only; installer skips this block via the [DEV] marker
#     so prod venvs don't pull pytest/coverage. Activate locally with:
#       pip install -r requirements-dev.txt
#     which sources this section.) ---------
# pytest==8.1.1
# pytest-asyncio==0.23.5
# httpx[cli]==0.27.0
